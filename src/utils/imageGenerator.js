/** Legacy entry point retained for old imports; this game uses DeepSeek text API only. */
export async function generateImageWithStabilityAI() {
  throw new Error('本版本仅接入 DeepSeek V4.1 Flash，不再提供外部图片生成接口')
}
